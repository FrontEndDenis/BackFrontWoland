.catalog-menu
	.catalog-menu__left
		h2.catalog-menu__title=category.name
		ul.catalog-menu__list
			each sub_category in category.get_child
				li(data-category-id=sub_category.id)
					a(href=sub_category.url)
						| #{sub_category.name}
						+icon('arrow-right')
		if category.get_child
			each sub_category in category.get_child
				.catalog-menu__catalog-submenu.catalog-submenu(data-category-id=sub_category.id)
					.catalog-submenu__wrap
						.catalog-submenu__item
							h3.catalog-submenu__title=sub_sub_categories.name
							ul.catalog-submenu__list
								each sub_sub_categories in sub_category.get_child
									li
										a(href=sub_sub_categories.url)=sub_sub_categories.name